# 特殊旅客协助交接资料服务

项目整理一次脱敏协助旅程的服务节点、岗位可见范围和接手时限。公开任务只保留服务类别，健康说明以独立引用表示；样例不包含真实姓名、证件号或联系方式。

在 Node.js 20 及以上环境执行 `npm test` 检查资料，再以 `npm start` 启动 `GET /health`。`docker compose up --build` 可验证容器入口。任何真实健康资料、短期查询凭证和加密密钥均由 `.gitignore` 隔离。
